import { WordShrotenPipe } from './word-shroten.pipe';

describe('WordShrotenPipe', () => {
  it('create an instance', () => {
    const pipe = new WordShrotenPipe();
    expect(pipe).toBeTruthy();
  });
});
